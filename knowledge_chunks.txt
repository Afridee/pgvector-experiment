---
title: Authentication — Required Headers for All Endpoints
tags: topic:auth, applies_to:all
content:
Every request to every report endpoint MUST include the following three headers.
You MUST inject these from the session/environment — do NOT ask the user for them.

  Authorization: Bearer {access_token}
  x-refresh-token: {refresh_token}
  x-validate-token: {validate_token}

If any of these tokens are missing from the session context, inform the user that they need to log in first.
Do NOT use placeholder values. Do NOT hard-code any token values.
---

---
title: SSS Report API — Endpoint and Method
tags: topic:endpoint, applies_to:sss_report, api_template
content:
To generate an SSS (Sales Summary Sheet) report, make a GET request to:

  {BASE_URL}/reports/sss-report

The base URL is environment-specific. Read it from the BASE_URL environment variable.
Do NOT hard-code the base URL.
You MUST include the authentication headers described in "Authentication — Required Headers for All Endpoints".
Content-Type is not required for GET requests.

Example request URL:

  GET {BASE_URL}/reports/sss-report?date=2025-06-15&ffType=2&pointFilter[]=1&pointFilter[]=2
---

---
title: SSS Report API — Required Parameters
tags: topic:parameters, applies_to:sss_report, business_rule
content:
Before calling the SSS Report API, you MUST collect ALL of the following from the user:

- date : string in YYYY-MM-DD format — the specific date for the report
- ffType : integer — the field force role type ID (e.g. 2)
- pointFilter[] : array of integers — one or more point IDs to filter by

All three parameters are required. If ANY are missing, ask the user before calling the API.
Do NOT use placeholder or default values.

These are sent as query string parameters. Because pointFilter is an array, repeat the key for each value:
  ?date=2025-06-15&ffType=2&pointFilter[]=1&pointFilter[]=2
---

---
title: SSS Report API — Response Format
tags: topic:response, applies_to:sss_report
content:
A successful SSS Report API response (HTTP 200) looks like:

```json
{
  "status": 200,
  "message": "SSS report generated successfully",
  "data": {
    "primaryData": [...],
    "retailerTarget": [...],
    "webEntryData": [...],
    "productData": [...],
    "fmcMemoCountData": [...]
  }
}
```

This endpoint returns structured JSON data directly — it does NOT generate a downloadable file.
Present the data to the user in a readable format.

If status is not 200, or if "data" is empty or missing, inform the user that no data was found for the given filters.
---

---
title: Query Manager Report API — Endpoint and Method
tags: topic:endpoint, applies_to:query_manager_report, api_template
content:
To generate a Query Manager Report (QMR), make a POST request to:

  {BASE_URL}/reports/query-manager-report

The base URL is environment-specific. Read it from the BASE_URL environment variable.
Do NOT hard-code the base URL.
You MUST include:
  Content-Type: application/json
  Authorization: Bearer {access_token}
  x-refresh-token: {refresh_token}
  x-validate-token: {validate_token}

Example request:

```json
{
  "startDate": "2026-01-08",
  "endDate": "2026-01-08",
  "regionFilter": [1, 2, 3],
  "subChannelFilter": [1, 2, 3],
  "productFilter": [1, 2, 14],
  "productType": "total",
  "reportType": ["stt", "memo"],
  "activeStatus": ["current"],
  "sttCriteria": { "sign": ">", "number": 0 },
  "memoCriteria": { "sign": ">", "number": 0 }
}
```
---

---
title: Query Manager Report API — Required Parameters
tags: topic:parameters, applies_to:query_manager_report, business_rule
content:
Before calling the Query Manager Report API, you MUST collect ALL of the following from the user:

- startDate : string in YYYY-MM-DD format — start of the reporting period
- endDate : string in YYYY-MM-DD format — end of the reporting period (inclusive)
- regionFilter : array of integers — one or more region IDs
- subChannelFilter : array of integers — one or more sub-channel IDs
- productFilter : array of integers — one or more product IDs
- productType : string — one of: "total", "sku", "brand", "family", "segment"
- reportType : array of strings — one or both of: "stt", "memo"

If ANY of these are missing, ask the user before calling the API.
Do NOT use placeholder or default values.
---

---
title: Query Manager Report API — Optional Parameters
tags: topic:parameters, applies_to:query_manager_report
content:
The following fields are optional for the Query Manager Report API:

- activeStatus : array of strings — filter by retailer active status. Use ["current"] for active retailers, ["inactive"] for inactive, or omit to include all.
- sttCriteria : object — filter rows by STT value. Shape: { "sign": ">"|"<"|">="|"<="|"=", "number": <integer> }. Default behaviour is no filter.
- memoCriteria : object — filter rows by memo count. Shape: { "sign": ">"|"<"|">="|"<="|"=", "number": <integer> }. Default behaviour is no filter.
- requestId : string — a UUID to track the report request; used for email notification. Can be omitted for direct calls.
---

---
title: Query Manager Report API — Response and Download Link
tags: topic:response, applies_to:query_manager_report
content:
A successful Query Manager Report API response (HTTP 200) looks like:

```json
{
  "status": 200,
  "message": "QMR report generated successfully",
  "data": {
    "fileUrl": "https://<azure-blob-url>/reports/<requestId>/<filename>.xlsx",
    "filename": "<filename>.xlsx"
  }
}
```

If the response contains a "fileUrl" field, present it to the user as a clickable download link:

  📥 Your report is ready: [Download Report](<fileUrl>)

If "status" is not 200, or "data" is missing/empty, inform the user that the report could not be generated
and show the error message from the response.

Error response shape:
```json
{
  "status": 404,
  "message": "No data found for the given query",
  "data": {}
}
```
---

---
title: Route Wise STT Report API — Endpoint and Method
tags: topic:endpoint, applies_to:route_wise_stt_report, api_template
content:
To generate a Route Wise STT (Secondary Through Trade) report, make a POST request to:

  {BASE_URL}/reports/route-wise-stt-report

The base URL is environment-specific. Read it from the BASE_URL environment variable.
Do NOT hard-code the base URL.
You MUST include:
  Content-Type: application/json
  Authorization: Bearer {access_token}
  x-refresh-token: {refresh_token}
  x-validate-token: {validate_token}

Example request:

```json
{
  "startDate": "2026-02-25",
  "endDate": "2026-02-25",
  "type": "SKU",
  "location": "route",
  "pointFilter": [1],
  "productFilter": [1, 2, 14, 11]
}
```
---

---
title: Route Wise STT Report API — Required Parameters
tags: topic:parameters, applies_to:route_wise_stt_report, business_rule
content:
Before calling the Route Wise STT Report API, you MUST collect ALL of the following from the user:

- startDate : string in YYYY-MM-DD format — start of the reporting period
- endDate : string in YYYY-MM-DD format — end of the reporting period (inclusive)
- type : string — product grouping type. One of: "SKU", "Brand", "Family", "Segment"

At least one location filter is also required:
- pointFilter : array of integers — point IDs (most specific)

If startDate, endDate, or type are missing, ask the user before calling the API.
---

---
title: Route Wise STT Report API — Optional Parameters
tags: topic:parameters, applies_to:route_wise_stt_report
content:
The following fields are optional for the Route Wise STT Report API:

- regionFilter : array of integers — filter by region IDs
- areaFilter : array of integers — filter by area IDs
- distributorFilter : array of integers — filter by distributor/house IDs
- territoryFilter : array of integers — filter by territory IDs
- pointFilter : array of integers — filter by point IDs (if not provided as required filter)
- productFilter : array of integers — filter by product IDs matching the chosen "type"
- location : string — granularity of the report rows. One of: "route", "section". Defaults to route-level if omitted.
---

---
title: Route Wise STT Report API — Response and Download Link
tags: topic:response, applies_to:route_wise_stt_report
content:
A successful Route Wise STT Report API response (HTTP 200) looks like:

```json
{
  "status": 200,
  "message": "Route Wise STT Report Generated Successfully",
  "data": {
    "fileUrl": "https://<azure-blob-url>/reports/<requestId>/<filename>.xlsx",
    "filename": "<filename>.xlsx"
  }
}
```

If the response contains a "fileUrl" field, present it to the user as a clickable download link:

  📥 Your report is ready: [Download Report](<fileUrl>)

If "status" is not 200, or "data" is missing, inform the user that no data was found.

Error response shape:
```json
{
  "status": 404,
  "message": "No data found for the given query",
  "data": {}
}
```
---

---
title: Route Wise Memo Report API — Endpoint and Method
tags: topic:endpoint, applies_to:route_wise_memo_report, api_template
content:
To generate a Route Wise Memo report, make a POST request to:

  {BASE_URL}/reports/route-wise-memo-report

The base URL is environment-specific. Read it from the BASE_URL environment variable.
Do NOT hard-code the base URL.
You MUST include:
  Content-Type: application/json
  Authorization: Bearer {access_token}
  x-refresh-token: {refresh_token}
  x-validate-token: {validate_token}

Example request:

```json
{
  "startDate": "2026-02-25",
  "endDate": "2026-02-25",
  "type": "SKU",
  "location": "route",
  "regionFilter": [1],
  "productFilter": [1, 2, 14, 11]
}
```
---

---
title: Route Wise Memo Report API — Required Parameters
tags: topic:parameters, applies_to:route_wise_memo_report, business_rule
content:
Before calling the Route Wise Memo Report API, you MUST collect ALL of the following from the user:

- startDate : string in YYYY-MM-DD format — start of the reporting period
- endDate : string in YYYY-MM-DD format — end of the reporting period (inclusive)
- type : string — product grouping type. One of: "SKU", "Brand", "Family", "Segment"

At least one geographic filter should also be provided (regionFilter is most common):
- regionFilter : array of integers — region IDs

If startDate, endDate, or type are missing, ask the user before calling the API.
---

---
title: Route Wise Memo Report API — Optional Parameters
tags: topic:parameters, applies_to:route_wise_memo_report
content:
The following fields are optional for the Route Wise Memo Report API:

- regionFilter : array of integers — filter by region IDs
- areaFilter : array of integers — filter by area IDs
- distributorFilter : array of integers — filter by distributor/house IDs
- territoryFilter : array of integers — filter by territory IDs
- pointFilter : array of integers — filter by point IDs
- productFilter : array of integers — filter by product IDs matching the chosen "type"
- location : string — granularity of the report rows. One of: "route", "section". Defaults to route-level if omitted.
---

---
title: Route Wise Memo Report API — Response and Download Link
tags: topic:response, applies_to:route_wise_memo_report
content:
A successful Route Wise Memo Report API response (HTTP 200) looks like:

```json
{
  "status": 200,
  "message": "Route wise Memo report generated successfully",
  "data": {
    "fileUrl": "https://<azure-blob-url>/reports/<requestId>/<filename>.xlsx",
    "filename": "<filename>.xlsx"
  }
}
```

If the response contains a "fileUrl" field, present it to the user as a clickable download link:

  📥 Your report is ready: [Download Report](<fileUrl>)

If "status" is not 200, or "data" is missing, inform the user that no data was found.

Error response shape:
```json
{
  "status": 404,
  "message": "No data found for the given query",
  "data": {}
}
```
---

---
title: Survey Report API — Endpoint and Method
tags: topic:endpoint, applies_to:survey_report, api_template
content:
To generate a Survey Report, make a POST request to:

  {BASE_URL}/reports/survey-report

The base URL is environment-specific. Read it from the BASE_URL environment variable.
Do NOT hard-code the base URL.
You MUST include:
  Content-Type: application/json
  Authorization: Bearer {access_token}
  x-refresh-token: {refresh_token}
  x-validate-token: {validate_token}

Example request:

```json
{
  "startDate": "2025-06-01",
  "endDate": "2025-06-30",
  "surveyId": 5,
  "regionFilter": [1, 2],
  "pointFilter": [10, 11]
}
```
---

---
title: Survey Report API — Required Parameters
tags: topic:parameters, applies_to:survey_report, business_rule
content:
Before calling the Survey Report API, you MUST collect ALL of the following from the user:

- startDate : string in YYYY-MM-DD format — start of the survey reporting period
- endDate : string in YYYY-MM-DD format — end of the survey reporting period (inclusive)
- surveyId : integer — the ID of the survey to report on

If ANY of these are missing, ask the user before calling the API.
Do NOT use placeholder or default values.
---

---
title: Survey Report API — Optional Parameters
tags: topic:parameters, applies_to:survey_report
content:
The following fields are optional for the Survey Report API:

- regionFilter : array of integers — filter by region IDs
- areaFilter : array of integers — filter by area IDs
- distributorFilter : array of integers — filter by distributor/house IDs
- territoryFilter : array of integers — filter by territory IDs
- pointFilter : array of integers — filter by point IDs

If none of the location filters are provided, the report covers all locations the issuer has access to.
---

---
title: Survey Report API — Response and Download Link
tags: topic:response, applies_to:survey_report
content:
A successful Survey Report API response (HTTP 200) looks like:

```json
{
  "status": 200,
  "message": "Survey report generated successfully",
  "data": {
    "fileUrl": "https://<azure-blob-url>/reports/<requestId>/<filename>.xlsx",
    "filename": "<filename>.xlsx"
  }
}
```

If the response contains a "fileUrl" in "data", present it to the user as a clickable download link:

  📥 Your report is ready: [Download Report](<fileUrl>)

If "status" is 404 or "data" is empty, inform the user that no survey data was found.

Error response shapes:
```json
{ "status": 404, "message": "No survey data found for the given query", "data": {} }
{ "status": 404, "message": "No data found for the given query", "data": {} }
```
---

---
title: STT Report API — Endpoint and Method
tags: topic:endpoint, applies_to:stt_report, api_template
content:
To generate a STT (Secondary Through Trade) Report, make a POST request to:

  {BASE_URL}/reports/stt-report

The base URL is environment-specific. Read it from the BASE_URL environment variable.
Do NOT hard-code the base URL.
You MUST include:
  Content-Type: application/json
  Authorization: Bearer {access_token}
  x-refresh-token: {refresh_token}
  x-validate-token: {validate_token}

Example request:

```json
{
  "startDate": "2025-08-01",
  "endDate": "2025-08-31",
  "productType": "sku",
  "dateGrouping": "month",
  "locationType": "area",
  "classificationType": "subchannel",
  "regionFilter": [1, 2],
  "productFilter": [1, 2, 14]
}
```
---

---
title: STT Report API — Required Parameters
tags: topic:parameters, applies_to:stt_report, business_rule
content:
Before calling the STT Report API, you MUST collect ALL of the following from the user:

- startDate : string in YYYY-MM-DD format — start of the reporting period
- endDate : string in YYYY-MM-DD format — end of the reporting period (inclusive)
- productType : string — product grouping type. One of: "sku", "brand", "family", "segment"

If ANY of these are missing, ask the user before calling the API.
Do NOT use placeholder or default values.
---

---
title: STT Report API — Optional Parameters
tags: topic:parameters, applies_to:stt_report
content:
The following fields are optional for the STT Report API:

- dateGrouping : string — how to group date columns. One of: "day", "week", "month", "year", "total". Defaults to "daily" if omitted.
- locationType : string — geographic hierarchy level for rows. One of: "region", "area", "distributor", "territory", "point". Defaults to point-level if omitted.
- classificationType : string — column classification. One of: "total", "channel", "subchannel". Defaults to total if omitted.
- regionFilter : array of integers — filter by region IDs
- areaFilter : array of integers — filter by area IDs
- distributorFilter : array of integers — filter by distributor/house IDs
- territoryFilter : array of integers — filter by territory IDs
- pointFilter : array of integers — filter by point IDs
- productFilter : array of integers — filter by product IDs (matching the chosen productType)
---

---
title: STT Report API — Response and Download Link
tags: topic:response, applies_to:stt_report
content:
A successful STT Report API response (HTTP 200) looks like:

```json
{
  "status": 200,
  "message": "STT Report Generated Successfully",
  "data": {
    "fileUrl": "https://<azure-blob-url>/reports/<requestId>/<filename>.xlsx",
    "filename": "<filename>.xlsx"
  }
}
```

If the response contains a "fileUrl" field, present it to the user as a clickable download link:

  📥 Your report is ready: [Download Report](<fileUrl>)

If "status" is 404 or "data" is empty, inform the user that no data was found.

Error response shape:
```json
{ "status": 404, "message": "No data found", "data": {} }
```
---

---
title: Memo Report API — Endpoint and Method
tags: topic:endpoint, applies_to:memo_report, api_template
content:
To generate a Memo Report (order memo / invoice count report), make a POST request to:

  {BASE_URL}/reports/memo-report

The base URL is environment-specific. Read it from the BASE_URL environment variable.
Do NOT hard-code the base URL.
You MUST include:
  Content-Type: application/json
  Authorization: Bearer {access_token}
  x-refresh-token: {refresh_token}
  x-validate-token: {validate_token}

Example request:

```json
{
  "startDate": "2025-08-01",
  "endDate": "2025-08-31",
  "productType": "sku",
  "dateGrouping": "month",
  "locationType": "area",
  "classificationType": "total",
  "regionFilter": [1, 2],
  "productFilter": [1, 2, 14]
}
```
---

---
title: Memo Report API — Required Parameters
tags: topic:parameters, applies_to:memo_report, business_rule
content:
Before calling the Memo Report API, you MUST collect ALL of the following from the user:

- startDate : string in YYYY-MM-DD format — start of the reporting period
- endDate : string in YYYY-MM-DD format — end of the reporting period (inclusive)
- productType : string — product grouping type. One of: "sku", "brand", "family", "segment"

If ANY of these are missing, ask the user before calling the API.
Do NOT use placeholder or default values.
---

---
title: Memo Report API — Optional Parameters
tags: topic:parameters, applies_to:memo_report
content:
The following fields are optional for the Memo Report API:

- dateGrouping : string — how to group date columns. One of: "day", "week", "month", "year", "total". Defaults to "daily" if omitted.
- locationType : string — geographic hierarchy level for rows. One of: "region", "area", "distributor", "territory", "point". Defaults to point-level if omitted.
- classificationType : string — column classification. One of: "total", "channel", "subchannel". Defaults to total if omitted.
- regionFilter : array of integers — filter by region IDs
- areaFilter : array of integers — filter by area IDs
- distributorFilter : array of integers — filter by distributor/house IDs
- territoryFilter : array of integers — filter by territory IDs
- pointFilter : array of integers — filter by point IDs
- productFilter : array of integers — filter by product IDs (matching the chosen productType)
---

---
title: Memo Report API — Response and Download Link
tags: topic:response, applies_to:memo_report
content:
A successful Memo Report API response (HTTP 200) looks like:

```json
{
  "status": 200,
  "message": "Memo Report Generated Successfully",
  "data": {
    "fileUrl": "https://<azure-blob-url>/reports/<requestId>/<filename>.xlsx",
    "filename": "<filename>.xlsx"
  }
}
```

If the response contains a "fileUrl" field, present it to the user as a clickable download link:

  📥 Your report is ready: [Download Report](<fileUrl>)

If "status" is 404 or "data" is empty, inform the user that no data was found.

Error response shape:
```json
{ "status": 404, "message": "No data found", "data": {} }
```
---

---
title: IRIS Gift Requisition Report API — Endpoint and Method
tags: topic:endpoint, applies_to:iris_gift_report, api_template
content:
To generate an IRIS Gift Requisition Report, make a POST request to:

  {BASE_URL}/reports/iris-gift-report

The base URL is environment-specific. Read it from the BASE_URL environment variable.
Do NOT hard-code the base URL.
You MUST include:
  Content-Type: application/json
  Authorization: Bearer {access_token}
  x-refresh-token: {refresh_token}
  x-validate-token: {validate_token}

Example request:

```json
{
  "startDate": "2025-01-01",
  "endDate": "2025-01-31"
}
```
---

---
title: IRIS Gift Requisition Report API — Required Parameters
tags: topic:parameters, applies_to:iris_gift_report, business_rule
content:
Before calling the IRIS Gift Requisition Report API, you MUST collect ALL of the following from the user:

- startDate : string in YYYY-MM-DD format — start of the reporting period (filters by submit_time)
- endDate : string in YYYY-MM-DD format — end of the reporting period (inclusive)

If ANY of these are missing, ask the user before calling the API.
Do NOT use placeholder or default values.

This report covers gift requisitions with statuses: NO_REQUISITION, INITIAL_REQUISITION,
PENDING_REQUEST_FOR_CHANGE, CHANGE_APPROVED, CONFIRMED.
---

---
title: IRIS Gift Requisition Report API — Response and Download Link
tags: topic:response, applies_to:iris_gift_report
content:
A successful IRIS Gift Requisition Report API response (HTTP 200) looks like:

```json
{
  "status": 200,
  "message": "Iris Gifts report generated successfully",
  "data": {
    "fileUrl": "https://<azure-blob-url>/reports/iris-gift-report/<filename>.xlsx",
    "filename": "<filename>.xlsx"
  }
}
```

If the response contains a "fileUrl" field, present it to the user as a clickable download link:

  📥 Your report is ready: [Download Report](<fileUrl>)

If "status" is 404, inform the user that no gift requisitions were found for the given date range.

Error response shapes:
```json
{ "status": 404, "message": "No Gift Requisition found for this year", "data": {} }
{ "status": 404, "message": "No data found for the given query", "data": {} }
```
---
